let main = pλ ε   : ℝ⁺,
              δ   : ℝ⁺
              .
              ε   : ℝ⁺[ε],
              δ   : ℝ⁺[δ],
              df :  𝕄 [L∞ , U | ★ , ℕ ∷ ℝ ∷ [] ]
              ⇒
  result ← gauss[ℝ⁺[1.0], ε, δ] <df> { real (rows df) };
  return result
in main
