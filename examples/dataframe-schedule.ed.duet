-- scheduling dataframe example
let main = pλ .
              p₁ : 𝔻𝔽[time:ℕ, busy₁:𝔹],
              p₂ : 𝔻𝔽[time:ℕ, busy₂:𝔹]
              ⇒
  firstDF ⧼time⧽ (filterDF (join[time] p₁ p₂) { x ⇒ !(x⧼busy₁⧽ ∨ x⧼busy₂⧽) })
in main
